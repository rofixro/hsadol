/**
 * 两数相加
 * @param num1 第一个数字
 * @param num2 第二个数字
 * @typeParam Number
 * @return(s) 返回两个数字相加之和
 */
export function sum(num1: number, num2: number): number {
  const a = 1;
  if (!Array.isArray(a)) {
    return num1 + num2 + a;
  } else {
    return num1 + num2 + a;
  }
}
